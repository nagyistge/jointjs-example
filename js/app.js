require(['resize'], function (resize) {
    //init.initControls(drag.graphControls);
    //init.initMainControls(devs.graph);
    resize();
});


//require(['init', 'drag', 'devs', 'resize'], function (init, drag, devs, resize) {
//    init.initControls(drag.graphControls);
//    //init.initMainControls(devs.graph);
//    console.log('resize', resize);
//    resize();
//});